name: Workflows lint
on:
  pull_request:
    branches: [ main ]

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      
      - name: Run
        uses: docker://rhysd/actionlint:latest
        with:
          args: -color
